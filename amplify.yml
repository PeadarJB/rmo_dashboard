version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 18 # <--- Add this line to set Node.js version
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*